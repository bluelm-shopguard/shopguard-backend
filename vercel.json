{
  "version": 2,
  "builds": [
    {
      "src": "newserver.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "newserver.py" }
  ]
}
